<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('homepage') }}">
    <i class="fas fa-clinic-medical"></i>
    GSB Project</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>


    <div class="collapse navbar-collapse" id="navbarColor02">
        {% if app.user %}
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">         
                    <a href="{{ path('medicaments_index') }}" class="nav-link">Médicaments</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('familles_index') }}">Familles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('composants_index') }}">Composants</a>
                </li>
                {% for role in app.user.RolesUser %}
                    {% if role.titre == "ROLE_ADMIN" %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('users_index') }}">Utilisateurs</a>
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        {{app.user.TitreFullName}}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria labelledby="accountDropdownLink">
                        <a href="{{ path('account_password') }}" class="dropdown-item">Modifier le mot de passe</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            
            </ul>
        {% endif %}
    </div>
</nav>

   <script>
            $('a').mouseover(function() {
                $(this).dropdown('toggle')
            })
        </script>

{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}